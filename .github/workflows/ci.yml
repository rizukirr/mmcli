name: CI

on:
  push:
  pull_request:

jobs:
  check:
    name: ${{ matrix.name }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: lint
            run: |
              uv sync
              uv run ruff check .
              uv run ruff format --check .
          - name: test
            run: |
              uv sync --extra test
              uv run pytest
          - name: build
            run: uv build
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - run: ${{ matrix.run }}
